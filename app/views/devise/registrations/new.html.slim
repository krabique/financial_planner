h2
  | Sign up
= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { multipart: true }) do |f|
  = f.error_notification
  .form-inputs
    = f.label :avatar do
      = f.file_field :avatar
      = f.hidden_field :avatar_cache
    = f.input :name, required: true, autofocus: true
    = f.input :email, required: true
    = f.input :password, required: true, hint: (I18n.t('devise.registrations.password_new_input_hint', minimum_password_length: @minimum_password_length) if @minimum_password_length)
    = f.input :password_confirmation, required: true
  .form-actions
    = f.button :submit, I18n.t('devise.registrations.sign_up'), class: 'btn btn-primary'
    hr
= render "devise/shared/links"
